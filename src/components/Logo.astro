---
interface Props {
  href?: string;
  class?: string;
}

const { href = "#top", class: className = "" } = Astro.props;
---

<a href={href} class:list={["inline-flex items-center", className]}>
  <img src="/logo.png" alt="surfskills.surf" class="h-10 w-auto" />
</a>
