#!/bin/sh
branch="$(git symbolic-ref --quiet --short HEAD 2>/dev/null || echo detached)"
if [ "$branch" = "main" ]; then
    echo "✋ Commit blocked: you're on 'main'. Create a feature branch: git switch -c my-feature"
    exit 1
fi
